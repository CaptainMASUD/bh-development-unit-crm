import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/maintenance.controller.js";const r=Router();r.post("/generate",requirePermission("manufacturing-maintenance:manage"),c.generateMaintenance);r.get("/plans",requirePermission("manufacturing-maintenance:view"),c.listMaintenancePlans);r.post("/plans",requirePermission("manufacturing-maintenance:manage"),c.createMaintenancePlan);r.get("/plans/:id",requirePermission("manufacturing-maintenance:view"),c.getMaintenancePlan);r.patch("/plans/:id",requirePermission("manufacturing-maintenance:manage"),c.updateMaintenancePlan);r.get("/orders",requirePermission("manufacturing-maintenance:view"),c.listMaintenanceOrders);r.post("/orders",requirePermission("manufacturing-maintenance:manage"),c.createMaintenanceOrder);r.get("/orders/:id",requirePermission("manufacturing-maintenance:view"),c.getMaintenanceOrder);r.patch("/orders/:id",requirePermission("manufacturing-maintenance:manage"),c.updateMaintenanceOrder);export default r;
