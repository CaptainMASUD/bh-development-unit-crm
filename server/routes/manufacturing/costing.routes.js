import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/manufacturingCost.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-cost:view"),c.listManufacturingCosts);r.get("/:id",requirePermission("manufacturing-cost:view"),c.getManufacturingCost);r.post("/order/:manufacturingOrderId/recalculate",requirePermission("manufacturing-cost:manage"),c.recalculateManufacturingCost);export default r;
