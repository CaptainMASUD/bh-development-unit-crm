import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/workOrder.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-work-order:view"),c.listWorkOrders);r.get("/:id",requirePermission("manufacturing-work-order:view"),c.getWorkOrder);r.patch("/:id",requirePermission("manufacturing-work-order:manage"),c.updateWorkOrder);r.post("/:id/start",requirePermission("manufacturing-work-order:execute"),c.startWork);r.post("/:id/pause",requirePermission("manufacturing-work-order:execute"),c.pauseWork);r.post("/:id/complete",requirePermission("manufacturing-work-order:execute"),c.completeWork);export default r;
