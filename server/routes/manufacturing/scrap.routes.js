import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/scrap.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-scrap:view"),c.listScrapEntrys);r.post("/",requirePermission("manufacturing-scrap:manage"),c.createScrapEntry);r.get("/:id",requirePermission("manufacturing-scrap:view"),c.getScrapEntry);r.patch("/:id",requirePermission("manufacturing-scrap:manage"),c.updateScrapEntry);r.delete("/:id",requirePermission("manufacturing-scrap:delete"),c.deleteScrapEntry);export default r;
