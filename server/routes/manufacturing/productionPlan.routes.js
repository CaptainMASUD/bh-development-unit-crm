import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/productionPlan.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-plan:view"),c.listProductionPlans);r.post("/",requirePermission("manufacturing-plan:manage"),c.createProductionPlan);r.post("/:id/approve",requirePermission("manufacturing-plan:approve"),c.approveProductionPlan);r.get("/:id",requirePermission("manufacturing-plan:view"),c.getProductionPlan);r.patch("/:id",requirePermission("manufacturing-plan:manage"),c.updateProductionPlan);r.delete("/:id",requirePermission("manufacturing-plan:delete"),c.deleteProductionPlan);export default r;
