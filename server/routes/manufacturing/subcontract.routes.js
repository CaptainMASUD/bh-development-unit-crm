import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/subcontract.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-subcontract:view"),c.listSubcontractOrders);r.post("/",requirePermission("manufacturing-subcontract:manage"),c.createSubcontractOrder);r.get("/:id",requirePermission("manufacturing-subcontract:view"),c.getSubcontractOrder);r.patch("/:id",requirePermission("manufacturing-subcontract:manage"),c.updateSubcontractOrder);r.delete("/:id",requirePermission("manufacturing-subcontract:delete"),c.deleteSubcontractOrder);export default r;
