import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/productionSchedule.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-schedule:view"),c.listProductionSchedules);r.post("/",requirePermission("manufacturing-schedule:manage"),c.createProductionSchedule);r.get("/:id",requirePermission("manufacturing-schedule:view"),c.getProductionSchedule);r.patch("/:id",requirePermission("manufacturing-schedule:manage"),c.updateProductionSchedule);r.delete("/:id",requirePermission("manufacturing-schedule:delete"),c.deleteProductionSchedule);export default r;
