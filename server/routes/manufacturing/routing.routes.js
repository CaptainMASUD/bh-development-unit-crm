import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/routing.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-routing:view"),c.listRoutings);r.post("/",requirePermission("manufacturing-routing:manage"),c.createRouting);r.get("/:id",requirePermission("manufacturing-routing:view"),c.getRouting);r.patch("/:id",requirePermission("manufacturing-routing:manage"),c.updateRouting);r.delete("/:id",requirePermission("manufacturing-routing:delete"),c.deleteRouting);export default r;
