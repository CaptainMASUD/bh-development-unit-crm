import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/materialIssue.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-material:view"),c.listMaterialIssues);r.post("/",requirePermission("manufacturing-material:manage"),c.createMaterialIssue);r.get("/:id",requirePermission("manufacturing-material:view"),c.getMaterialIssue);r.patch("/:id",requirePermission("manufacturing-material:manage"),c.updateMaterialIssue);r.post("/:id/post",requirePermission("manufacturing-material:post"),c.postMaterialIssue);export default r;
