import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/manufacturingReport.controller.js";const r=Router();r.get("/production-summary",requirePermission("manufacturing-report:view"),c.productionSummaryReport);r.get("/material-consumption",requirePermission("manufacturing-report:view"),c.materialConsumptionReport);r.get("/efficiency",requirePermission("manufacturing-report:view"),c.efficiencyReport);r.get("/scrap",requirePermission("manufacturing-report:view"),c.scrapReport);r.get("/traceability/:manufacturingOrderId",requirePermission("manufacturing-report:view"),c.traceabilityReport);export default r;
