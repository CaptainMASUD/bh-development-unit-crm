import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/workCenter.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-work-center:view"),c.listWorkCenters);r.post("/",requirePermission("manufacturing-work-center:manage"),c.createWorkCenter);r.get("/:id",requirePermission("manufacturing-work-center:view"),c.getWorkCenter);r.patch("/:id",requirePermission("manufacturing-work-center:manage"),c.updateWorkCenter);r.delete("/:id",requirePermission("manufacturing-work-center:delete"),c.deleteWorkCenter);export default r;
