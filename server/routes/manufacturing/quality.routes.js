import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/qualityInspection.controller.js";const r=Router();r.get("/inspections",requirePermission("manufacturing-quality:view"),c.listQualityInspections);r.post("/inspections",requirePermission("manufacturing-quality:manage"),c.createQualityInspection);r.get("/inspections/:id",requirePermission("manufacturing-quality:view"),c.getQualityInspection);r.patch("/inspections/:id",requirePermission("manufacturing-quality:manage"),c.updateQualityInspection);r.post("/inspections/:id/finalize",requirePermission("manufacturing-quality:approve"),c.finalizeQualityInspection);r.post("/inspections/:id/ncr",requirePermission("manufacturing-quality:manage"),c.createInspectionNcr);r.get("/ncr",requirePermission("manufacturing-quality:view"),c.listNonConformances);r.get("/ncr/:id",requirePermission("manufacturing-quality:view"),c.getNonConformance);r.patch("/ncr/:id",requirePermission("manufacturing-quality:manage"),c.updateNonConformance);export default r;
