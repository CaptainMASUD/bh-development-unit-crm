import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/mrp.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-mrp:view"),c.listMrpRuns);r.post("/run",requirePermission("manufacturing-mrp:manage"),c.executeMrp);r.get("/:id",requirePermission("manufacturing-mrp:view"),c.getMrpRun);export default r;
