import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/machine.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-machine:view"),c.listMachines);r.post("/",requirePermission("manufacturing-machine:manage"),c.createMachine);r.get("/:id",requirePermission("manufacturing-machine:view"),c.getMachine);r.patch("/:id",requirePermission("manufacturing-machine:manage"),c.updateMachine);r.delete("/:id",requirePermission("manufacturing-machine:delete"),c.deleteMachine);export default r;
