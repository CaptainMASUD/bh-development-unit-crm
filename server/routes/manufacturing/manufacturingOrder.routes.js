import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/manufacturingOrder.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-order:view"),c.listManufacturingOrders);r.post("/",requirePermission("manufacturing-order:manage"),c.createManufacturingOrder);r.post("/:id/release",requirePermission("manufacturing-order:release"),c.releaseOrder);r.post("/:id/cancel",requirePermission("manufacturing-order:manage"),c.cancelManufacturingOrder);r.get("/:id",requirePermission("manufacturing-order:view"),c.getManufacturingOrder);r.patch("/:id",requirePermission("manufacturing-order:manage"),c.updateManufacturingOrder);r.delete("/:id",requirePermission("manufacturing-order:delete"),c.deleteManufacturingOrder);export default r;
