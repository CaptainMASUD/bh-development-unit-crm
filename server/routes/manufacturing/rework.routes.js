import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/rework.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-rework:view"),c.listReworkOrders);r.post("/",requirePermission("manufacturing-rework:manage"),c.createReworkOrder);r.get("/:id",requirePermission("manufacturing-rework:view"),c.getReworkOrder);r.patch("/:id",requirePermission("manufacturing-rework:manage"),c.updateReworkOrder);r.delete("/:id",requirePermission("manufacturing-rework:delete"),c.deleteReworkOrder);export default r;
