import {Router} from "express"; import {requirePermission} from "../../middleware/auth.middleware.js"; import * as c from "../../controllers/manufacturing/billOfMaterial.controller.js"; const r=Router();
r.get("/",requirePermission("manufacturing-bom:view"),c.listBillOfMaterials);r.post("/",requirePermission("manufacturing-bom:manage"),c.createBillOfMaterial);r.get("/:id/explode",requirePermission("manufacturing-bom:view"),c.explodeBillOfMaterial);r.post("/:id/activate",requirePermission("manufacturing-bom:approve"),c.activateBillOfMaterial);r.get("/:id",requirePermission("manufacturing-bom:view"),c.getBillOfMaterial);r.patch("/:id",requirePermission("manufacturing-bom:manage"),c.updateBillOfMaterial);r.delete("/:id",requirePermission("manufacturing-bom:delete"),c.deleteBillOfMaterial);export default r;
