import {Router} from "express";import {requirePermission} from "../../middleware/auth.middleware.js";import * as c from "../../controllers/manufacturing/productionEntry.controller.js";const r=Router();r.get("/",requirePermission("manufacturing-production:view"),c.listProductionEntries);r.post("/",requirePermission("manufacturing-production:manage"),c.createProductionEntry);r.get("/:id",requirePermission("manufacturing-production:view"),c.getProductionEntry);r.patch("/:id",requirePermission("manufacturing-production:manage"),c.updateProductionEntry);r.post("/:id/post",requirePermission("manufacturing-production:post"),c.postProductionEntry);export default r;
